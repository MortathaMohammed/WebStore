@model List<Size>

@{
    ViewData["Title"] = "Sizes";
}
<div class="mt-3 mx-3">
    <a class="btn btn-primary " asp-controller="Size" asp-action="CreateSize">أضافة قياس</a>
</div>
<table class="table table-hover mt-5">
    <thead>
        <tr>
            <th scope="col">ألاسم</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Name</td>
                <td>
                    <a class="btn btn-warning mx-3 col-md-3" asp-action="EditSize" asp-route-id="@item.Id">تعديل</a>

                    <a class="btn btn-danger mx-3 col-md-3" asp-action="DeleteSize" asp-route-id="@item.Id">حذف</a>

                </td>
            </tr>

        }
    </tbody>
</table>